 <% book.book_comments.each do |book_comment| %>
          <p><%= image_tag book_comment.user.get_profile_image, size: "50x50" %></p>

          <%= link_to book_comment.user.name, user_path(book_comment.user), class: "mb-3" %>
           <%= book_comment.comment %>
          <% if book_comment.user == current_user %>
            <%= link_to "Destroy", book_book_comment_path(book_comment.book, book_comment), class: "btn btn-sm btn-danger", method: :delete, remote: true %>
          <% end %>
          <% end %>
 